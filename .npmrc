# Flatten node_modules layout — required for Docker builds where tools like
# `nest build` / `next build` expect dependencies directly under the package's
# node_modules rather than symlinked from the pnpm store.
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
